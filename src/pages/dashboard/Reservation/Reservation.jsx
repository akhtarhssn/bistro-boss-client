import SectionHeader from "../../../components/SectionHeader/SectionHeader";

const Reservation = () => {
  return (
    <div className="container mx-auto p-5 md:p-0">
      <SectionHeader title={"Book a table"} subTitle={"reservation"} />
      <h2>Reservation</h2>
    </div>
  );
};

export default Reservation;
